"""Stage: explain — global and per-zip SHAP explanations (task M5).

Global SHAP summary plot to reports/figures/; explain_zip(zip, month) returns
top-k feature contributions for the dashboard in < 1s per zip.
"""


def run(config: dict) -> None:
    raise NotImplementedError("explain — implemented in spec_model.md task M5")
