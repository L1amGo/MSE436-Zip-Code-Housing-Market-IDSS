"""Streamlit decision dashboard (spec_dashboard.md).

Presentation layer only: layout, widgets, caching, and plotting. Every number on
screen comes from a `model/` call — no scoring, feature engineering, or training
logic lives here.
"""
