"""Stage: join (implemented in a later task)."""


def run(config: dict, force: bool = False) -> None:
    raise NotImplementedError(
        "stage 'join' is not implemented yet - see spec.md for its task definition"
    )
